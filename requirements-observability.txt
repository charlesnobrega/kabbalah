# Optional external telemetry exporters.
#
# Install only when Prometheus export is being enabled:
#   python -m pip install -r requirements-observability.txt

opentelemetry-exporter-prometheus==0.64b0
prometheus-client==0.25.0
