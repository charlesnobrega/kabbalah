# Legacy Code Archive

This directory contains older code snapshots moved out of active import paths.

The active runtime package is `src/kabbalah`. Do not import modules from this archive in production code or tests unless a migration task explicitly promotes them back into `src/kabbalah`.
